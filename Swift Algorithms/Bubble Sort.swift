//
//  Bubble Sort.swift
//  Swift Algorithms
//
//  Created by Wingfield, James (PEPW) on 03/11/2023.
//

import Foundation

func bubbleSort{unSortedArray) {

    var swapped = TRUE
    temp = ""
    for i in range(0,len(unSortedArray-1){
        if unSortedArray[i] > unSortedArray[i+1]{
            temp = unSortedArray[i+1]
            unSortedArray[i+1] = unSortedArray[i]
            unSortedArray[i] = temp
            swapped = TRUE
    
    var sortedArray = unSortedArray
    return sortedArray
        }
    }
    
    
    
    
}
